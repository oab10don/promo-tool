import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* 外部画像を使用する場合は remotePatterns を追加してください。
     例:
     images: {
       remotePatterns: [
         { protocol: "https", hostname: "example.com", pathname: "/images/**" },
       ],
     },
  */
};

export default nextConfig;
